
  <view class="card" style="width: {{width}}px">
    <view style="width: {{width}}px;">
      <image style="width: {{width}}px;" bindtap="previewImageInter" src="http://api.dujin.org/bing/1366.php" alt="Bing每日图片超高清"/>
    </view>
    <!-- <block wx:for="{{news}}" wx:key="index"> -->
    <view class="cards">
      <view class="card-view-information">
        <text style="color: RoyalBlue; margin-bottom: 15rpx">#10.30上新#</text>
        <text class="card-text">占字符占字符占字符占字符占字符占字符占字符占字符占字符占字符占字符占字符占字符占字符占字符占字符占字符占字符占字符占字符占字符占字符占字符占字符占字符占字符占字符占字符占字符占字符</text>
        <view class="card-view-photo">
          <block  wx:for="{{imaUrl}}" wx:key="{{index}}">
            <image class="card-photo" data-index="{{index}}" bindtap="previewImage" src="{{item.url}}"/>
          </block>
        </view>
      </view>
    </view>
    <!-- </block> -->
    <text style="color: lightgrey; font-size: 11px; margin-bottom: 20rpx">----到底啦----</text>
  </view>